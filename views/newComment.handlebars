{{#each posts.posts as |post|}}
{{> postDetails}}
{{/each}}
{{#each comments as |comment|}}
{{> comment}}
{{/each}}
{{#each posts.posts as |post|}}
<section class="card">
  <div class="card-header">
    <h3>New Comment</h3>
  </div>
  <div class="card-body form-signin">
    <form action="/api/post/new-comment/{{id}}" method="post">
      <div class="form-floating">
        <textarea id="floatingText" name="commentContent" placeholder="Comment Content"></textarea>
      </div>
      <button class="w-100 btn btn-lg btn-primary" type="submit">Submit</button>
    </form>
  </div>
</section>
{{/each}}
